# Quantum Sand Project Templates NumPy

* [001](/quantumsand-project-templates/quantumsand-project-templates-numpy/quantumsand-project-template-numpy-001.ipynb)
```
Create an `ndarray` of 1 million random values with NumPy; Delta time for Python `sum()`; Delta time for NumPy `sum()`; Delta time ratio between Python `sum()` and NumPy `sum()`
```

More to follow.
